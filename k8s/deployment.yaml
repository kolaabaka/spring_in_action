apiVersion: v1
kind: Service
metadata:
  name: sprint-in-action-service
spec:
  type: NodePort  # open port on nodes
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080  # available 30080
  selector:
    app: sprint-in-action